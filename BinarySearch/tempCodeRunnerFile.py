while m < cur:
#   mid += 1
#   idx = bisect(trees, mid)
#   cur = sum(tree-mid for tree in trees[idx:])
#   if m > cur:
#     break
#   elif m < cur:
#     ans = mid
#     l = mid+1
#   else:
#     ans = mid
#     break